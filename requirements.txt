#this file contains all the required packages that must be installed 

requests 
plotly.express #seems to install pandas and other packages as well as plotly
pandas
python-dotenv
pytest #for testing 

# sendgrid==6.5.0 - I am not using sendgrid, using mailgun instead